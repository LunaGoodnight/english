import { Header } from "../common/Header";
import { SiteNav } from "../common/SiteNav";

export const sites = () => {
  return (
    <div>
      <Header />
      <SiteNav />
    </div>
  );
};

export default sites;
